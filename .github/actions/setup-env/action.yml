name: "Setup Python & Dependencies"
description: "Sets up Python 3.12 with pip caching and installs required project dependencies with SHA pinning"

inputs:
  python-version:
    description: "Python version to set up"
    required: false
    default: "3.12"

runs:
  using: "composite"
  steps:
    - name: Setup Python ${{ inputs.python-version }}
      uses: actions/setup-python@39cd14742d08025d6053865ea99933f070b40e4e # v5.3.0
      with:
        python-version: ${{ inputs.python-version }}
        cache: "pip"

    - name: Install Dependencies
      shell: bash
      run: |
        python -m pip install --upgrade pip
        pip install -e . ruff mypy pytest detect-secrets pylint
