{
  "hooks": {
    "PreCommandExecute": [
      {
        "type": "command",
        "command": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"${PLUGIN_ROOT}/hooks/firewall_hook.ps1\" -CommandText \"${COMMAND}\""
      }
    ]
  }
}
